# Общее для всех сред
spring.application.name=practice
spring.jpa.properties.hibernate.format_sql=true
spring.flyway.baseline-on-migrate=true

# Настройки для разработки по умолчанию (можно оставить)
spring.profiles.active=dev